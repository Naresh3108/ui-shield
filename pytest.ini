[pytest]
testpaths = tests
addopts = -v --tb=short --strict-markers

markers =
    smoke: basic homepage and navigation tests
    regression: full test suite
    functional: feature tests like login and products
    cross_browser: tests that run on multiple browsers